"""
Imports file so we can import any prompt in this directory like this:
    from app.prompts import PROMPT
Note, we don't have to mention the containing file's name because that is
mentioned here.
"""

from app.prompts.extract_features import EXTRACT_FEATURES_PROMPT
